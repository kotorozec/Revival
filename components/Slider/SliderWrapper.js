const SliderWrapper = ({children}) => {
    return (
        <div className={"h-w-full rounded-lg bg-white p-12 flex justify-center items-center"}>
            {children}
        </div>
    )
}

export default SliderWrapper